# Розгортання та відновлення

[English](deployment-safety.md) | Українська

Поправка користувача від 2026-09-12 трактує явний запит створити/оновити/імпортувати визначений застосунок розробки/тестування як дозвіл на необхідний імпорт у цих межах. Після перевірки конкретного плану агент може записати наявну вказівку як короткостроковий локальний deploy grant для точного проєкту, цілі та плану; він має зберегти сторонню політику й запис дозволу та видалити grant після спроби. Не запитуйте той самий дозвіл двічі й не зупиняйтеся на плані. Це фіксує вже надану згоду, не вигадує її, не дозволяє сторонні записи в базу й не замінює захищені production-підписи. Ідентичність, SQL-копія, перевірки розбіжностей, координація, фіксація коду та обробка невідомого результату залишаються обов’язковими.

Для перевірки лише застосунку можна використати явно дозволений ізольований профіль із компілятором, реальним звірянням SQL лише для читання та спостереженнями вбудованого браузера, зберігши початковий профіль обов’язкових наборів і його історичні результати. Відсутність налаштованих автоматизованих наборів означає, що вони не запускалися, а не що SQL/E2E пройшли. Див. [навичку тестування](../plugins/apexrest-apex/skills/apexrest-test/SKILL.md#application-only-verification-scope).

Розгортання працює на чистій підтримуваній інсталяції APEX без службових таблиць APEXREST. Типовий режим — `deploymentControl: "local"` (також коли поле не задано). Він зберігає незмінну історію міграцій і володіння схемою в `$APEXREST_HOME/deployment-control/`; запити до службових таблиць чи DDL у цьому режимі не виконуються. Окремі посилання на з’єднання для читання та розгортання дають змогу мінімізувати привілеї.

`deploy plan --env dev --out plans/dev.json` читає точні унікальну назву бази, сервіс, схему, робочий простір та ідентичність застосунку, експортує цільовий вміст, хешує код/конфігурацію/інструментарій і виконує валідацію справжнім компілятором. План діє 30 хвилин. Планування експортує в локальний проміжний каталог, але не імпортує в БД. Повний імпорт включає сторінки й спільні компоненти; зміна однієї сторінки все одно є повним імпортом. Непідтримувані компоненти має блокувати справжній компілятор; покриття не є універсальним.

`deploy apply --plan plans/dev.json` повторно перевіряє план, зовнішню політику, поточну ціль і компілятор. Для наявної цілі до будь-яких записів у застосунок чи схему створюється скопійований SQL-експорт із контрольною сумою. Після другої перевірки розбіжностей код фіксується й хешується. Впорядковані незмінні міграції стійко записують `started` перед виконанням (типово локальний файл, у database mode — рядок БД); змінені контрольні суми або невирішені результати блокують наступні плани. Скрипти пакетів виконуються після міграцій. Перша помилка SQLcl зупиняє виконання; транзакційність DDL не припускається.

Локальний режим виконує послідовно runners зі спільним керованим home для тієї самої БД/сервісу/parsing schema, включно з різними застосунками цієї схеми. Підтверджено завершеного власника на тому самому хості можна замінити лише у фазі підготовки. Після невідомого результату володіння записом зберігається й ніколи автоматично не знімається після збою. План прив’язує backend, обсяг та ідентичність локального сховища. Зберігайте deployment-control: нова машина чи видалена історія не можуть визначити з Oracle раніше застосовані міграції. Для кількох машин використовуйте один стійкий runner/home та зовнішнє послідовне виконання CI або явно виберіть database mode. Локальні файлові блокування не гарантують розподіленої координації БД.

Необов’язковий `deploymentControl: "database"` зберігає наявний журнал БД і оренду рядка. Лише цей явно вибраний режим потребує перевірених таблиць `templates/database-control/install.sql`. Setup їх не створює. Heartbeat — 20 секунд, TTL — 90 секунд; можна повторно отримати лише прострочену оренду підготовки. Початкова реалізація БД між runners усе ще потребує реальної конкурентної перевірки Oracle. Непомітного переходу з database mode немає, оскільки він міг би відкинути історію міграцій.

Застосунок імпортується з явними workspace/schema/ID, ідентичність перевіряється, обов’язкові набори виконуються. Порожній/пропущений/заблокований обов’язковий набір провалює перевірку. Скасування, тайм-аут або втрата оренди після запису дають `outcome_unknown`; автоматичного повторного apply немає. `deploy status --run UUID` читає ціль/експорт/історію для звіряння й ніколи не перетворює невизначеність на успіх.

utPLSQL потрібен лише коли SQL-набір вибраний/обов’язковий. Проєкт лише застосунку може розгортатися без нього; не видаляйте справді обов’язкових наборів заради успішної перевірки. Підтримка чистого APEX змінює сховище контролю, а не вимоги до дозволів чи якості. Плани, створені до появи поля координації, потрібно згенерувати заново.

`deploy restore-plan --backup UUID --out plans/restore.json` перевіряє копію та поточну ціль, створює новий план і потребує grant для його точного digest. Apply створює нову копію, потім відновлює один повний нерозділений Oracle-експорт `f<ID>.sql` із явною ціллю встановлення. Розділені/невідомі формати блокуються. Це відновлює лише метадані APEX; бізнес-дані й руйнівні зміни схеми потребують окремого DBA-відновлення. Загальні руйнівні/привілейовані зміни та зміни автентифікації залишаються заблокованими до явного процесу відновлення.

Production потребує `CI=true`, `APEXREST_APPROVAL_FILE` і `APEXREST_APPROVAL_PUBLIC_KEY_FILE` на захищеному runner. Зовнішній підпис Ed25519 покриває канонічний JSON із `planDigest`, `targetDigest`, `expiresAt`, `reviewer`. Репозиторій не може надавати чи змінювати ключ перевірки runner. `approved=true` відхиляється строгими схемами. Жодна операція, доступна агенту, не підписує схвалень.
