# Next actions and exact dependencies

Repository preparation is implemented: English product documentation, accessible infographics, direct GitHub marketplace installation, a checked-in self-contained native bundle, source/hash freshness gates and cross-platform CI. See [getting started](getting-started.md) and the [current native installation evidence](evidence/native-repository.json). This installable beta retains the stable release dependencies below.

The [app 175 dashboard](dashboard-app175.md) is imported and checked in the authenticated Codex in-app browser. Five real read-only source scenarios pass 33 assertions; UI checks cover period/store filters, dates, charts and a real empty state. The authorized application-only profile ran no automated suites; the original empty SQL/E2E failures remain recorded. No business-table writes occurred. Oracle restore/fault injection and the full integration fixture matrix below remain open.

The [connected optimization follow-up](optimization-review.md#connected-apex-workflow-follow-up) is complete: plan median 20.08 → 9.73 seconds over three samples per build; one optimized import took 33.4 seconds, compared with approximately 71–73 seconds in the earlier trial. Fresh target checks, backup and drift protection remain. That optimization checkpoint passed 64 unit, five contract and five packaging tests, typecheck, lint, build and eight skill validators. The current counts are recorded in [local-checks.json](evidence/local-checks.json). The earlier streaming/reference optimization evidence remains separate. New-app timing, other database sizes, native-host context tokens and other platforms are not measured by this run.

For future UI changes, follow the [in-app browser rule](../plugins/apexrest-apex/skills/apexrest-test/SKILL.md#in-app-browser-verification). Reuse the authenticated target and check relevant behavior after loading completes. Keep source/browser observations distinct from automated tests. For future development/test requests, complete the necessary authorized import without requesting the same permission again; production still requires protected external approval.

1. **Extend connected Oracle verification beyond the dashboard.** Actual app 175 plan/apply/export now pass without service tables, including SQL backup and post-import component/MMD preservation. Local durable control is the default. Remaining full integration prerequisites are separately authorized shared-schema mutations, a reviewed utPLSQL setup for applicable SQL suites, and a configured authenticated CRUD fixture. Browser access is now authenticated for the dashboard; anonymous Developer-authentication rejection does not establish a login blocker. The app name does not establish that the whole schema is disposable.
2. **Finish the real integration fixture matrix.** Extend the app 175 changed-page/shared-LOV/MMD and repeated-final-export evidence to broader component fixtures; execute unsupported-component rejection, SQL backup restore, checksum mismatch, lock contention from two runners, disconnect during DDL/import, cancellation and reconciliation. The current happy-path harness is explicitly partial and does not certify these cases. Validate CRM DOM selectors, negative input, run-specific cleanup and SQL test results against the deployed app.
3. **Run each client platform.** Execute clean-machine native/plugin setup and bootstrap tests on Linux x64, Windows x64 and WSL2. Check missing Node/Java/SQLcl, Oracle license refusal, proxy/CA, browser OS dependencies, cache miss/corruption, concurrent/interrupted setup, version upgrade and uninstall preservation. Windows PowerShell is implemented but was not executed on this macOS host. Project-only enablement needs a supported, actually verified Codex mechanism.
4. **Strengthen remaining safety evidence.** Exercise foreign-plugin preservation, cross-version cache refresh/rollback and partial install recovery. Validate process-tree termination and externally signed production approval in a protected runner inaccessible to agent/project code. Browser download integrity currently relies on pinned Playwright rather than independently pinned CDN payload SHA-256. Redaction does not constitute exhaustive sensitive-business-data detection.
5. **Prepare an authorized immutable release.** Keep reviewed sources in the user-selected [apexrest-dev/apexrest-codex](https://github.com/apexrest-dev/apexrest-codex) repository. Complete all required evidence for the exact release source digest, configure protected CI and external signing/publication credentials, verify npm ownership and support channels, then enable publisher configuration. Rebuild artifacts and pass readiness before publishing. Source storage is authorized; the beta remains unsigned and the release publisher is disabled.
6. **Optional sandbox.** Implement a legitimate, pinned Linux x64 Oracle Free/APEX/ORDS/utPLSQL tuple with approved provisioning, private generated credentials and persistent volumes. Until it really provisions and passes tests, keep `supported:false`; ordinary down must preserve data.

Independent local implementation is delivered in code, templates, scripts, schemas, documentation and built artifacts. The limitations above are not marked completed merely because the host, compiler or fixture tests pass.
