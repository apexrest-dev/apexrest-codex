# Next actions and exact dependencies

English | [Українська](next-actions.uk.md)

## Minor beta 0.4.0-beta.1 — 2026-09-21

Track this version's checks in [minor-beta local evidence](evidence/minor-040-local.json). Preserve the earlier 253-test automation report with its original build identity. After loading the updated plugin in a new Codex task, verify current-session edits and compare matched tasks using actual host counters; Oracle and application-browser checks remain separately authorized work. The authorized commit/push targets `main`; no Git tag, GitHub release or npm publication is part of it. Keep existing deployment safeguards and stable-release gates intact. See [release notes](release-notes.md).

## Verify Codex automation — 2026-09-21

Use the [programmatic workflow](codex-automation.md): consume the single-work project summary, batch related metadata requests and reuse returned `jobId` values after the built-in MCP wait. Retrieve full artifacts only when needed. [Local validation](evidence/codex-automation-local.json) for `0.3.0-beta.1+codex.20260921193125` passed 253 tests, typecheck and lint; [built CLI/MCP checks](evidence/codex-automation-runtime.json) confirmed one-call success/failure and the compact current-session context. Preserve these checks, then compare matched real Codex tasks using host usage counters. Separately verify authorized Oracle batches, long-running deployment outcomes and the affected pages in the selected browser. Local call-count/payload checks do not establish billed-token savings or native end-to-end latency. Keep deployment safety gates and required suites unchanged; use App Server only for explicitly enabled teams.

## Verify the context optimization — 2026-09-21

[Local checks](evidence/plugin-efficiency-local.json) for `0.3.0-beta.1+codex.20260921165734` passed: 194 unit, 22 contract and 14 packaging tests, typecheck, lint, 46 documentation pairs, the 30-page site build and plugin sync/integrity checks. The local plugin is reinstalled with all 246 installed file hashes verified; built CLI/MCP current-session responses and a one-call worker wait passed. Start a new Codex task to load it. Earlier verification records below retain their original scope. Keep all 24 tools, effective input constraints, authorization and deployment safeguards. For long operations use `jobs.status` with `waitSeconds: 25` (allowed 0–30); one MCP call waits internally, while omitted/0 retains immediate status.

The [context measurement](evidence/plugin-context-local.json) records 42,003 → 31,789 bytes (-24.32%) across all 13 skill files and 24,966 → 22,499 bytes (-9.88%) for the tool catalog. The [reference benchmark](evidence/reference-ranking-cache-local.json) records 12.93 → 3.09 ms for 24 varied searches with identical full-response SHA-256. Preserve these local regression checks. Use matched native tasks and actual host counters before claiming end-to-end latency or billed-token savings; aggregate skill bytes do not imply every skill loads for every task. Oracle timing remains a separate authorized measurement. See the [optimization review](optimization-review.md).

Single mode now uses the user's existing Codex session. [Local checks](evidence/current-session-local.json) passed: 188 unit, 22 contract and 14 packaging tests, typecheck, lint, 46 documentation pairs, the 30-page site, plugin integrity/fresh build, built CLI/stdio MCP receipts and retries, and a synthetic Chrome panel bridge with an empty agent roster in all three response encodings. The installed plugin is `0.3.0-beta.1+codex.20260921164648`. Start a new Codex task to load it, then verify an actual desktop task with direct edits, relevant checks and the saved browser preference. Native end-to-end and Oracle/application-browser checks remain open. Use actual host counters and matched tasks before claiming token or latency savings; the plugin does not independently meter the current chat. Earlier verification records below retain their original scope.

The earlier single-agent-default revision added explicit Settings opt-in. Refresh native installation/discovery for the current-session revision; run a team fixture only with explicit multi-agent opt-in. Compare the same tasks before/after with actual host counters if billing conclusions are needed. Local payload-byte reductions and in-app settings observations are recorded in the [implementation status](implementation-status.md#single-by-default-and-bounded-context--2026-09-21). No model sessions or Oracle work were run for this change.

Token context fixes are implemented: prerequisite exits, per-role context changes, focused reference dependencies and recoverable bounded MCP responses. Keep the new regression budgets and embedded-panel bridge check in subsequent validation. The remaining efficiency step is a fixed native task comparison with per-role input/cache/output/reasoning counters and unchanged quality gates; local byte reductions do not establish billed-token savings. See [current implementation](implementation-status.md#token-context-optimization--2026-09-21).

The [README Cloud overview](../README.md#ords-sql-a-path-to-codex-cloud) includes the announcement infographic and describes phone delegation and an intended developer/reviewer/QA sequence. Before presenting that APEXREST Cloud team as verified, exercise it end to end on an authorized development/test target, including Slack task dispatch, agent coordination and the applicable application tests. ChatGPT mobile Remote retains its awake, online computer requirement.

The [Codex Cloud guide](codex-cloud.md) and reusable bootstrap examples are documented and locally checked. Next, exercise setup and cached maintenance in an actual Linux Cloud environment with authorized credential retention, its proxy/CA and an identified development/test ORDS target. Record agent-phase identity verification separately from native skill/MCP discovery or browser checks. Preserve private deployment history/backups and coordinate independent containers before authorizing imports; local documentation checks do not qualify Cloud deployment support.

The current minor update is `0.3.0-beta.1` dated 2026-09-21; see the [release notes](release-notes.md), [version-specific local checks](evidence/minor-update-030.json) and [native checks](evidence/minor-update-030-native.json). Start a new Codex task after reinstalling to load the updated plugin. Git commit/push to `main`, npm publication and stable qualification are separate steps. No Git tag is created; npm publication remains pending. Earlier token-optimization, Oracle and UI evidence keeps its original build identity.

The [connection settings](panel.md) passed [20 built-panel checks and Codex in-app form saving](evidence/connection-settings-local.json) with synthetic credentials. [Native verification](evidence/connection-settings-native.json) discovered 24 installed tools and confirmed the exact `saved-connections` MCP response against 21 real local SQLcl names, published as a count only. Regression, lint, typecheck and 45 documentation language pairs passed. Next verify this UI and saved-store discovery on Windows/Linux. These checks made no database login attempt and do not extend the earlier ORDS import/export evidence.

The authorized [ORDS](ords.md) CLI no-op import/export round trip and real SQL backup passed on a private test target; all 21 re-exported files matched byte for byte. See the [connected evidence](evidence/ords-connected.json). Next, within separately authorized test scope, cover changed imports, additional static-file/shared-component/MMD variants, SQL restore, interrupted-response reconciliation and Windows. Application browser verification is deferred at the user's explicit request; the present scope is import/export. Do not extend database changes or reuse unrelated credentials to obtain further evidence.

[Execution and browser modes](work-modes.md) are implemented and checked locally. Start a new Codex chat to pick up the updated installed plugin's tools and instructions. The next separate check is an authorized APEX application with actual Google/SSO login in the selected browser and available controls for it. URL opening does not prove authentication or tests; automated Playwright keeps separate state. Native Windows/Linux, embedded MCP UI and a new Codex in-app browser observation remain unverified.

[Chat-driven work and team Auto models](chat-workflow.md) are implemented. Verify implicit skill choice and direct single execution in fresh desktop and CLI chats; for teams, extend interrupted-host resumption and unavailable-catalog coverage. Compare tokens/latency on matched tasks. Keep independent reviews mandatory for explicitly enabled teams and distinguish cumulative cached input from cost. Current native execution and in-app panel observations are recorded in the implementation status.

The [illustrated agent guide](agent-workflow.md) is complete in both languages, with four actual panel captures and a workflow diagram. Refresh these images when the UI or workflow changes; retain their isolated-coding-fixture captions. Connected Oracle team execution, embedded MCP rendering and Windows verification remain separate open checks.

The [opt-in team](team.md) and [development panel](panel.md) are implemented. Extend verification to authorized APEX application tasks, artifact-writing QA suites, Windows console/process recovery and embedded MCP UI rendering. In-app browser rendering and native App Server sessions are separate evidence from a custom desktop sidebar or parent-child session attachment. The [source audit](codex-integration.md) records the actual extension points. Refresh the installed bundle through the CLI after source changes; a new Codex task is needed to load updated skills/tools.

SQLcl CLI / official MCP selection is implemented and checked locally, including the macOS pseudo-terminal and real offline SQLcl. Next verify authorized connected queries, exports, imports, transaction boundaries and unknown-outcome reconciliation in both modes on an identified development/test target. Review Oracle MCP audit behavior and verify Windows. These connected/native checks remain open; see [implementation and evidence](implementation-status.md#sqlcl-cli--official-sqlcl-mcp--2026-09-16).

The [2026-09-16 skill review](evidence/skill-review-local.json) found no confirmed instruction defects. The next skill-quality check remains a fixed set of complete authoring tasks followed by authorized connected SQL/import and browser verification; passing local retrieval, packaging and offline compiler checks does not close those gaps.

The user authorized npm publication. The beta archive is prepared and its CLI installation was checked. Complete npm login with an account authorized for `@apexrest`, verify scope access, publish the reviewed archive with public access and the `beta` tag, then verify registry metadata and installation from the registry. Publication is currently blocked by `ENEEDAUTH`. The stale local Codex marketplace registration was repaired for the verified panel bundle, as recorded below.

The [APEXlang TUI dashboard](tui.md#apexlang-dashboard) is implemented and locally verified: bundled catalogue, search, scrolling, focus switching and adaptive table. Next verify macOS/Windows terminals. Documented type counts do not establish support for every compiler variant; retain separate actual Oracle evidence for that. See the [local report](evidence/tui-apexlang-dashboard-local.json).

Installation documentation is updated in the [README](../README.md), [getting-started guide](getting-started.md#install-with-the-terminal-menu) and [site installation page](../site/content/install.md). [Local verification](evidence/installation-documentation-local.json) covers documentation and the generated site. Next, exercise the documented flow on fresh supported environments; the text revision does not close live native-host or Oracle verification gaps.

The [existing local registration update](implementation-status.md#existing-local-plugin-registration) resolves the reported ownership conflict for a dedicated local APEXREST marketplace without a managed receipt. [Registration evidence](evidence/setup-registration-local.json) records passing local checks, isolated native migration/repeated setup and the authorized repair in the current user profile. Cache files and MCP were verified; prior source, unrelated registrations/settings and existing toolchains were preserved without downloads or Oracle work. Next, complete macOS/Windows checks and the live native failure, unknown-outcome recovery and cross-machine concurrency matrices.

The focused [TUI](tui.md) contains only tool/plugin installation and removal plus saved SQLcl connection listing/testing and SQLcl CLI/MCP selection. The current flow executes after Enter on review, without Preview only or Approve changes toggles; see [direct-execution verification](evidence/tui-direct-run-local.json). [Evidence](evidence/tui-focused-local.json) separates local tests, Linux terminal execution and real listing of an empty SQLcl store. Next verify a connection with working saved credentials and macOS/Windows terminals. SQLcl fixture responses are not Oracle connection evidence.

The [Setup source-selection fix](implementation-status.md#setup-source-selection) limits installation to the selected plugin and preserves symlink rejection within that bundle. Historical [local evidence](evidence/setup-source-local.json) records passing regression contracts, installer fixtures with a Codex stub and successful CLI/Linux terminal previews without native installation or Codex configuration writes. The later native registration checks are recorded separately above. Source-selection behavior on macOS/Windows remains to be verified; the earlier fixtures do not establish native-host or Oracle behavior.

The **All functions** skill displays the APEXREST menu in conversation when sent `Use $apexrest-menu`; it does not add a permanent sidebar or top-menu button. The 2026-09-13 missing-menu investigation confirmed that the plugin is installed and enabled, all ten skills are exposed to the active conversation, and the diagnostic MCP tool responds. Next, confirm that the user can open the conversational menu and inspect discovery through the desktop `@` picker or **Plugins → Installed**. Native visual rendering and the cause of the reported missing entry remain unverified; host discovery alone does not resolve that report. See [menu navigation](getting-started.md#all-functions-in-the-codex-plugin-menu). The development installation uses a reviewed local build and requires no publication for these instructions.

The **Install dependencies** plugin command is implemented and packaged; use `$apexrest-install-dependencies` after installing the updated bundle. Native discovery is checked in an isolated Linux profile. Next, inspect the command’s rendered menu in the Codex app and exercise authorized real dependency downloads, license refusal and browser OS-dependency handling on clean supported platforms. This change does not refresh Oracle integration evidence or install dependencies into the user’s normal environment. See [command verification](implementation-status.md#dependency-installation-command).

The transparent APEXREST symbol without lettering is wired into the repository plugin and bilingual documentation. After installing this updated bundle, inspect the plugin card, details page and composer icon in Codex. Site rendering and package asset checks do not establish native UI rendering. Publication remains a separate authorized action.

The local Codex installation and global CLI now use the verified panel bundle, and the stale marketplace root is repaired. Start a new Codex task to pick up its twelve skills and twenty-one tools. Plugin card, composer-icon and Website-field rendering were not rechecked in this panel run. Distribution publication remains a separate authorized action.

APEXlang retrieval optimization is implemented and locally checked; see [its measured scope](apexlang-optimization.md). The next quality step is a broader fixed set of Codex authoring tasks with compiler attempts, elapsed time and context usage recorded, followed by authorized connected/browser checks. The twelve retrieval cases and three compiled compositions do not replace these task-level evaluations. Codex is the sole product target; portable-package release work is retired.

The repository provides English and Ukrainian documentation, accessible architecture/deployment diagrams in both languages, direct GitHub marketplace installation, a self-contained native bundle, integrity gates and cross-platform CI. [Getting started](getting-started.md) covers installation. [Implementation status](implementation-status.md), [local checks](evidence/local-checks.json) and [native installation evidence](evidence/native-repository.json) state the actual scope and source digest of each verification run.

Keep both documentation languages synchronized for future changes and run `npm run docs:check`, the site build and packaging checks. Documentation checks and site browser observations do not refresh Oracle or native-host evidence.

The installable beta retains these stable release dependencies:

1. **Configure a dedicated connected integration environment.** Identify a disposable application and explicit database/workspace/schema identity, supply named local SQLcl connections, and obtain authorization for the particular mutations the fixture requires. Configure authenticated browser access and utPLSQL only for applicable SQL suites. APEXREST service tables are not deployment prerequisites; local control remains the default.
2. **Complete the connected fixture matrix.** Verify export/adopt, no-op and changed-component import/export round trips, metadata/shared-component preservation, unsupported-component rejection, SQL backup and restore, migration checksums, runner contention, disconnect/cancellation and unknown-outcome reconciliation. Add authenticated CRUD, negative validation and run-specific cleanup. The happy-path integration harness alone does not certify these failure cases.
3. **Verify each native client platform.** Run clean-machine Codex installation and bootstrap on Linux x64, Windows x64 and WSL2, and refresh the macOS lifecycle for the release source. Cover missing dependencies, license refusal, proxy/CA, browser OS dependencies, cache miss/corruption, interrupted/concurrent setup, upgrades and uninstall preservation. Hosted fixture/packaging CI does not replace native-host tests. Project-only enablement needs a supported, verified Codex mechanism.
4. **Complete protection and recovery evidence.** Exercise unrelated-plugin preservation, cross-version cache refresh/rollback, partial install recovery and process-tree termination. Validate externally signed production approval on a protected runner inaccessible to project or agent code. Browser payload verification currently relies on pinned Playwright. Redaction does not establish exhaustive sensitive-data detection.
5. **Prepare an authorized immutable release.** Gather all required evidence against the exact release source digest, configure protected CI and external signing/publication credentials, and verify support channels and any optional npm ownership. Enable publishing only for the authorized release, rebuild artifacts and pass readiness before publication. The beta is currently unsigned and the publisher is disabled.
6. **Optional sandbox.** Implement and verify a legitimate pinned Linux Oracle Free/APEX/ORDS/utPLSQL stack with approved provisioning, private credentials and persistent volumes. Keep `supported:false` until it works; ordinary shutdown must preserve data.

For future visible changes, follow the [in-app browser rule](../plugins/apexrest-apex/skills/apexrest-test/SKILL.md#in-app-browser-verification). Check affected behavior after loading completes and keep observations separate from automated results. An identified development/test application request includes its necessary scoped import; production still requires protected external approval.

Implementation, local fixtures, native-host checks and connected integration remain separate. Missing or stale evidence cannot become a passing release gate through a documentation update.
