# Implementation status — local beta 0.1.0-beta.1

The original build specification remains unchanged. This is working code and built local artifacts, **not a fully completed or stable-verified product**. Every A01–A28 criterion has separate implementation, verification, evidence and blocker fields in [acceptance.json](acceptance.json).

| Area                | Implemented                                                                                                                                                                                                                                                              | Actually verified                                                                                                                                                                                            | Remaining boundary                                                                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Native plugin       | Two separate layouts, one identity, eight skills, bundled CLI/MCP, installer-owned native registration                                                                                                                                                                   | Codex 0.154.0 on macOS arm64: ZIP outside checkout, discovery, exact MCP calls, real generate/validate jobs, enable/disable, repeated offline setup, product uninstall                                       | Portable MCP is not discovered by this host; other host/platform versions unverified                                                                |
| Client installation | Bash/PowerShell, pinned hashes, safe ZIP/tar, user-local Node/Java/SQLcl/Playwright, license/elevation separation, locks/recovery, update/uninstall commands                                                                                                             | Real macOS client setup, Chromium launch, repeat offline setup, settings sentinel, integrity/traversal/concurrent/dead-owner fixture tests                                                                   | Windows/Linux/WSL2 execution and complete cross-version/interrupt/rollback matrix incomplete; project-only Codex scope blocked                      |
| APEXlang            | Real SQLcl adapter, generate/export/adopt/validate/diff, staged non-overwrite, MMD preservation, references, metadata queries                                                                                                                                            | Real blank/CRM compilation; connected app 175 adopt, validation, zero-diff repeated export, native MCP export and actual local page-title edit preservation; SQLcl 26.1.2.132.1334, Java 21, MMD 26.1.0+3102 | App 175 post-import page/shared-LOV/MMD preservation now passes; broader component and unsupported-component fixtures remain open                   |
| Deployment          | Strict immutable plans, source/config/toolchain hashes, explicit identity, drift checks, SQL backup, frozen sources, migrations, default local control, optional DB lease, journals, unknown-outcome reconciliation, separate restore plan, external production approval | Fixtures plus actual CLI/native plans and built MCP app 175 imports, target checks and SQL backups without service tables                                                                                    | App 175 apply/backup now passes; restore and failure injection remain unverified. Independent machines need external serialization/optional DB mode |
| Application tests   | utPLSQL JUnit runner, guarded Playwright/browser/API helpers, local interactive auth, synthetic CRM CRUD and negative test, required gates, private reports                                                                                                              | Test parsing/gates, actual empty-suite rejection and Chromium request to app 175                                                                                                                             | Authenticated in-app dashboard filters/empty state pass; no live automated SQL/CRUD suite result is claimed, and no UT package is visible           |
| Release and website | Local native/runtime ZIPs, installers, SHA-256, SBOM/notices, optional external signature, provenance, gated CI, Markdown site, local publishing adaptor                                                                                                                 | Mandatory local commands; deterministic ZIPs, schema validation, package escape scan, site links and Chromium desktop/mobile search/layout checks                                                            | Unsigned beta source distribution; no signed tagged release or Linux/Windows native-host evidence                                                   |
| Sandbox             | Capability/status diagnostics; fail-closed unsupported profile                                                                                                                                                                                                           | No provisioned Oracle sandbox claimed                                                                                                                                                                        | Complete licensed Linux DB/APEX/ORDS/utPLSQL provisioning implementation and evidence absent                                                        |

## Earlier local verification checkpoint

The counts and source-bound reports in this section describe the earlier checkpoint. Current repository-distribution verification is recorded below.

[evidence/local-checks.json](evidence/local-checks.json) is refreshed by each consolidated local run and records its current Node version, source digest, exit codes, durations and log hashes. Full console logs remain private under `.apexrest/check-logs/`.

- `npm ci --offline`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm run site:build`, `npm run release:dry-run`: successful local runs on Node 24.21.0.
- `npm run test:unit`: 46 passing tests, including clean plan/apply, durable history, interrupted-write retention and separate Node runner contention. Oracle writes are labelled fixtures, not DB integration results.
- `npm run test:contracts`: 3 passing real stdio MCP/CLI contract tests; no Oracle target involved.
- `npm run test:installers`: 10 passing download/archive/platform fixtures. An escaping tar-link test found an unhandled callback exception; the adapter now returns a classified failure and the test passes.
- `npm run test:packaging`: 5 passing native-schema/self-contained/ZIP/integrity/site checks.
- `npm run test:native-codex`: actual isolated host evidence in [native-codex-compat.json](evidence/native-codex-compat.json), including real SQLcl jobs through the installed MCP.
- `node scripts/oracle-smoke.mjs`: actual compiler results for both templates in [oracle-local.json](evidence/oracle-local.json).
- `node scripts/setup-smoke.mjs`: actual local artifact setup, repeat offline setup and managed launcher in [setup-local.json](evidence/setup-local.json).
- `npm run test:integration`: the recorded full harness run remains **blocked, exit 3**. A subsequent user-supplied `megasport` / app 175 target now passes connected read-only verification; utPLSQL for the required SQL suite, authenticated runtime access and mutation scope were unavailable at that earlier checkpoint. Subsequent authorized dashboard imports and authenticated observations are recorded below. Service tables are no longer prerequisites. No database write was attempted by that harness run. The happy-path harness also requires additional fault-injection/preservation evidence before it can contribute a full stable gate.
- `npm run check-release-readiness`: intentionally **not ready, exit 3**. Missing platform/native and connected Oracle evidence must not become green through skipped jobs.

## Connected follow-up: megasport / application 175

[Verification report](verification-app175.md) and [machine-readable evidence](evidence/oracle-app175-readonly.json) record the actual follow-up. APEX is 26.1.1; the three-page app exports to 21 APEXlang files, compiles successfully and produces a 96,227-byte SQL backup. A fresh Codex profile discovers 14 installed MCP tools; exact metadata/validate/export calls pass against the supplied target. The original native plan failed with ORA-00942; the subsequent clean-APEX change now passes actual CLI and native MCP deploy plans without either service table. At that read-only checkpoint, anonymous browser access failed and required empty SQL/E2E suites correctly failed their gate. Later authenticated dashboard/import evidence is recorded below.

Read/export stability is not a post-import round trip. The user amendment is now implemented in code and three skills, with 64 current local tests and rebuilt native ZIPs. See [clean APEX evidence](evidence/clean-apex-deployment.json). At that historical checkpoint Oracle apply was unexecuted; the dashboard follow-up below records subsequent authorized imports.

Earlier complete native lifecycle, full setup and two-template compiler reports retain their original source digest; they were not relabelled as current. This change has current connected native metadata/validate/export/plan evidence. Stable readiness remains false, including stale full lifecycle/compiler evidence and missing platform/full Oracle gates.

## In-app browser rule follow-up (2026-09-12)

Implemented the user-requested conditional Codex in-app browser check in `apexrest-test`, with links from the APEXlang, deploy, debug and review skills and a repository rule in `AGENTS.md`. For user-visible changes, inspect the affected pages and interactions when the browser and deployed change are available. Record observed results separately from automated tests; missing browser/access leaves that verification incomplete and does not authorize deployment or database writes.

Verified locally: all eight skills pass `quick_validate.py`; `npm run build` succeeds; the built Codex plugin passes `validate_plugin.py`; all five `npm run test:packaging` checks pass. All source skills match both built profiles, and the four new skill links resolve. The original specification is unchanged. These are instruction/package checks: no live in-app application check was performed for this documentation change, A19 remains blocked, and installed plugin caches and release archives were not refreshed.

## Optimization follow-up (2026-09-12)

[Review and measurements](optimization-review.md), [ADR 008](adr/008-runtime-optimization.md) and [local evidence](evidence/optimization-local.json) record shared CLI/MCP chunks, lazy installer setup, streaming downloads/checksums and an invalidated-on-change reference index. Measured runtime size decreased 51.6%; peak RSS for the synthetic 128 MiB download/cache check decreased 68.7%; repeated reference search decreased from 3.942 ms to 0.422 ms. The first reference lookup has a small indexing cost. No Oracle or native-host speedup is inferred from these local samples.

Current local checks pass: 48 unit, 14 installer, 5 CLI/MCP contract and 5 packaging tests, plus typecheck, lint and build. Both copied package profiles resolve references and complete a synthetic local unit-test job through direct MCP and CLI MCP entrypoints outside the checkout. The eight skills and original specification are unchanged by this follow-up. Earlier complete native lifecycle and Oracle evidence remains tied to its earlier source digest; stable readiness remains false.

## Dashboard and connected optimization follow-up (2026-09-12)

[Application 175](dashboard-app175.md) is deployed through the built stdio MCP, with five read-only data scenarios and 33 passing assertions. Four successful application imports include the final unchanged-source verification of the optimized runtime. SQL backups, target identity, both drift checks and local durable coordination were enforced. All 20 unaffected files, authentication and MMD match the original export; all 21 final exported files match before/after the last import. No migrations, business-table writes or authentication changes occurred. Every task-scoped grant was removed after its attempt.

The user explicitly authorized the application-only verification profile. Its automated suite list is empty, so no automated SQL/E2E tests ran; original required-suite failures and profile remain preserved. Authenticated in-app observations cover default/7/90-day views, one store, dates, chart refresh and a real empty state. Default-viewport rendering was inspected; separate mobile breakpoint, CRUD/negative-validation and restore/fault-injection evidence remain open.

[Connected speed evidence](evidence/optimization-oracle-app175.json) records a plan median of 20.08 → 9.73 seconds over three samples per build and one optimized import in 33.4 seconds versus approximately 71–73 seconds in the earlier trial. One fresh target query replaces three SQLcl sessions; successful help is reused with version/runtime invalidation; independent preflight reads overlap. Historical ORA text returned as JSON data no longer produces a false command failure. Fresh target, compiler, drift, backup and write barriers remain intact.

Current checks: 64 unit tests, five CLI/MCP contract tests, five packaging tests, typecheck, lint, build and eight skill validators pass. Sixteen new tests cover cache invalidation, errors and concurrency. Four skills now avoid repeated discovery and duplicate compile-before-plan calls and include conditional native dashboard guidance. The original specification remains unchanged. Built local profiles are refreshed; no global APEXREST install was present to reinstall. This is actual Oracle/built-MCP evidence, not a new installed native-host lifecycle run, release archive or full integration certification.

## Local deliverable files

`dist/releases/` contains exact filenames and hashes in `release-manifest.json`, local install commands in `install-local.txt`, and `release-readiness.json`. `site-dist/` is the static `/codex/` deployment directory. `site-dist.zip` is built from the same release metadata. No Oracle binaries, private browser state or raw local-machine evidence are included in public packages.

## Source repository follow-up (2026-09-12)

The user designated [apexrest-dev/apexrest-codex](https://github.com/apexrest-dev/apexrest-codex) for source storage. The initial source commit `0997b6f` was pushed to the public repository; Git origin, project rules, README links and publisher repository metadata now point there. Local private runtime, connection/auth state, backups and dashboard data remain ignored. Subsequent source commits and pushes remain within this authorization; release publication remains disabled. Repository-only metadata changes do not relabel earlier runtime measurements or native evidence as fresh.

No tag, remote release, npm publication, apex.rest upload or server provisioning is part of this source synchronization. The authorized app 175 imports above remain the connected application mutations; broader release and database work requires its own scope and evidence.

## Repository distribution and documentation (2026-09-12)

The repository now includes `.agents/plugins/marketplace.json` and a complete native bundle under `plugins/apexrest-apex/`. Direct Codex registration requires Node 24 on `PATH`; users do not need npm dependencies or a TypeScript build. Native startup uses Codex's supported plugin-relative `cwd`, and project-scoped MCP tools require an explicit absolute project path. CLI relative-path behavior remains unchanged.

The [native repository smoke report](evidence/native-repository.json) records actual isolated Codex 0.154.0 / macOS arm64 installation, eight skills, 14 connected tools, reference search, doctor and project inspection outside the installed cache. This read-only native check performs no Oracle import and does not replace the broader historical lifecycle or connected Oracle evidence.

`npm run plugin:sync` builds and refreshes the checked-in bundle. `npm run plugin:check` verifies source freshness, every file hash, native marketplace parity and agreement with a fresh build. Linux, macOS and Windows CI runs this gate after building; `.gitattributes` keeps source and bundle line endings consistent. Platform-specific optional license notices are inventoried but excluded from cross-platform fresh-build comparison.

Public documentation is English, including the README, quickstart, configuration, architecture, testing, security, contribution guide and all 28 acceptance descriptions. The original build specification remains unchanged as historical input. Three accessible SVG infographics show the architecture, deployment checks and qualified measured performance. The static site renders 14 routes with tables, diagrams, lists, safe Markdown, validated links and heading anchors.

The [current local check report](evidence/local-checks.json) records 64 unit, 14 installer, six CLI/MCP contract and 12 packaging tests passing on Node 24.21.0, with lint, typecheck, build, bundle verification, site generation and release dry-run successful. All eight skills and the native manifest pass their validators. [In-app site observations](evidence/repository-site-browser.json) confirm overview/install navigation, search, loaded diagrams, mobile navigation and no horizontal overflow at the observed desktop and narrow CSS widths. Repository distribution does not satisfy remaining stable gates: no new Oracle recovery/fault-injection certification, Linux/Windows native-host lifecycle, protected production approval or signed stable release is claimed. The release publisher remains disabled.
