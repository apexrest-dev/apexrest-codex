# Application 175 dashboard — deployed and checked

The user-authorized test dashboard is imported into application 175. The actual built APEXREST stdio MCP server completed four imports: the initial dashboard, two filter fixes, and an unchanged-source verification of the optimized deployment runtime. This is direct MCP/Oracle evidence, not refreshed native Codex plugin discovery. See [sanitized evidence](evidence/dashboard-app175.json).

Only page 1 changes: period/store filters, five operational metrics, daily net sales, and top-ten store/brand charts. The other 20 exported files, including authentication, shared LOVs and Oracle MMD, match the original baseline byte for byte after import. All 21 final exported files match before and after the last unchanged-source import. Native export normalizes page syntax; preservation is checked against exported components, not a claim that authored page text remains identical.

The source is the real daily sales/returns aggregate. Signed amounts and quantities are summed directly; chart totals and KPI values reconcile to independent SELECTs in five read-only scenarios with 33 assertions. Monetary currency remains unconfirmed, so the page claims no currency symbol. Oracle SQLcl 26.1.2 compilation passes. The query harness encodes non-ASCII SQL literals as equivalent `to_char(unistr(...))` values to avoid SQLcl stdin encoding corruption. Raw business results, source artifacts and authorization records remain in ignored private directories.

Authenticated Codex in-app checks cover the 30-day default, 7/90-day filters, one store, and a real store with no operations in the selected period. KPI values and dates reconcile; the store chart narrows correctly; empty results show zero counts and undefined ratios. The final page uses native dynamic actions to refresh four regions and submits the filter items with every region request. The date range belongs to refreshed KPI metadata. Initial chart "No data" messages disappear after asynchronous loading. Default-viewport screenshots confirm readable cards and charts; a separate mobile breakpoint was not tested. Reload and the 7-day filter passed again after the optimized import.

The latest deployment completed with a fresh SQL backup, verified target identity, both target drift checks, frozen source, and local durable coordination. Its exact-plan temporary grant was removed after the attempt. No database migration, business-table DML, authentication change or service-table setup occurred.

The original inspection profile and its empty required SQL/E2E failures remain unchanged. The user explicitly authorized an isolated application-only profile using compiler, real query and in-app checks; that profile declares no automated suites. **No automated SQL/E2E suite ran.** The deployment journal's testing milestone is not evidence that a suite passed. Authenticated dashboard inspection does not complete A19's CRUD/negative-validation matrix, and Oracle restore/fault-injection gates remain open.

The trial's measured [runtime and workflow optimizations](optimization-review.md#connected-apex-workflow-follow-up) are implemented and rebuilt locally. No release or global plugin reinstall is claimed.
